# DB 접속
mysql -u root -p

# 데이터베이스 보기
show databases;

# 데이터베이스 선택
use jwt_project_db;

# 테이블 목록 확인
show tables;

# 테이블 구조 확인
DESC users;

# Create(회원가입 예제)
INSERT INTO users (username, password, name, age, phone, email)
VALUES ('chulsu123', 'password', '김철수', 25, '010-1234-5678', 'chulsu@example.com');

# Read(로그인 및 정보 조회 예제)
SELECT * FROM users
WHERE username = 'chulsu123';

# Update(회원정보 수정 예제)
UPDATE users
SET phone = '010-9999-8888'
WHERE id = 1;

# Delete(회원탈퇴 예제)
DELETE FROM users WHERE id = 1;
